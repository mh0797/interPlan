gc_pgp_planner:
  _target_: nuplan.planning.simulation.planner.ml_planner.ml_planner.MLPlanner
  _convert_: 'all'
  _recursive_: False

  model_config: ${gc_pgp_model} # Dictionary key from existing planner model config (e.g. raster_model)
  checkpoint_path: ${oc.env:NUPLAN_EXP_ROOT}/checkpoints/gc_pgp_checkpoint.ckpt  # Path to trained model checkpoint

